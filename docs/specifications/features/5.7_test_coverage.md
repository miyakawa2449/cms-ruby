# Phase 5.7: テストカバレッジ向上 - 機能仕様書

**Phase**: 5.7  
**機能名**: テストカバレッジ向上  
**優先度**: 中  
**実施期間**: 2026-01-26 〜 2026-01-31（6日間）  
**ステータス**: 未着手  
**作成日**: 2026-01-18

---

## 📋 概要

AI機能、メディアライブラリ、データベースエクスポート/インポート機能のテストカバレッジを向上させ、コード品質と信頼性を高めます。テストピラミッドに基づき、ユニットテスト、統合テスト、E2Eテストを体系的に整備します。

---

## 🎯 目標

### 定量目標
- **テストカバレッジ**: 85%以上
- **テスト総数**: 150件以上
- **テスト実行時間**: 30分以内
- **CI/CD統合**: GitHub Actions自動テスト

### 定性目標
- テストの保守性向上
- バグ検出率向上
- リファクタリングの安全性向上
- 開発速度の向上

---

## 🚀 主要機能

### 1. AI機能テスト追加

#### 1.1 各Suggester/Generatorのユニットテスト（60件）

**対象サービス**:
- TitleSuggester（10件）
- SummaryGenerator（10件）
- TagSuggester（10件）
- SlugGenerator（10件）
- SeoMetaGenerator（10件）
- StructureSuggester（10件）

**テスト項目**:
- プロンプト生成の正確性
- レスポンスパースの正確性
- エラーハンドリング（タイムアウト、APIエラー）
- リトライ機能
- 使用量トラッキング
- モデル選択の正確性
- キャッシュ機能
- エラーログ出力

**モック戦略**:
- BedrockClientをモック化
- 外部API依存を排除
- WebMockでHTTPリクエストをスタブ化

#### 1.2 AI基盤テスト（20件）

**BedrockClient（10件）**:
- 正常系: 成功レスポンス
- 異常系: タイムアウト、APIエラー、ネットワークエラー
- リトライ機能（最大3回、指数バックオフ）
- エラーログ出力
- トークン使用量計測

**UsageTracker（5件）**:
- 使用量記録
- 統計情報取得
- 日別/月別集計
- コスト計算

**ModelSelector（5件）**:
- モデル選択ロジック
- フォールバック機能
- モデル設定管理

---

### 2. 統合テスト強化（60件）

#### 2.1 記事CRUD全フローテスト（20件）

**作成フロー（5件）**:
1. 記事作成ページ表示
2. AI機能でタイトル提案
3. AI機能で要約生成
4. AI機能でタグ提案
5. 記事保存 → 確認

**編集フロー（5件）**:
1. 記事編集ページ表示
2. カテゴリ変更
3. タグ変更
4. 画像アップロード
5. 更新 → 確認

**削除フロー（5件）**:
1. 記事削除
2. キャッシュ無効化確認
3. 関連データ削除確認
4. Active Storage削除確認
5. リダイレクト確認

**公開フロー（5件）**:
1. 下書き保存
2. 公開
3. フロントエンド表示確認
4. 非公開
5. アクセス拒否確認

#### 2.2 認証・認可テスト（15件）

**ログイン・ログアウト（5件）**:
- 正常ログイン
- 不正ログイン
- ログアウト
- セッション維持
- Remember me機能

**アクセス拒否（5件）**:
- 非認証ユーザーのアクセス拒否
- 管理画面へのアクセス拒否
- API認証エラー
- CSRF保護
- XSS保護

**セッション管理（5件）**:
- セッション作成
- セッション更新
- セッション削除
- セッションタイムアウト
- 同時ログイン制御

#### 2.3 APIエンドポイントテスト（25件）

**記事API（10件）**:
- GET /api/articles（一覧取得）
- GET /api/articles/:id（詳細取得）
- POST /api/articles（作成）
- PATCH /api/articles/:id（更新）
- DELETE /api/articles/:id（削除）
- GET /api/articles/search（検索）
- GET /api/articles/published（公開記事）
- GET /api/articles/drafts（下書き）
- エラーレスポンス
- ページネーション

**カテゴリAPI（5件）**:
- GET /api/categories（一覧取得）
- GET /api/categories/:id（詳細取得）
- POST /api/categories（作成）
- PATCH /api/categories/:id（更新）
- DELETE /api/categories/:id（削除）

**タグAPI（5件）**:
- GET /api/tags（一覧取得）
- GET /api/tags/:id（詳細取得）
- POST /api/tags（作成）
- PATCH /api/tags/:id（更新）
- DELETE /api/tags/:id（削除）

**AI API（5件）**:
- POST /api/ai/suggest_title（タイトル提案）
- POST /api/ai/generate_summary（要約生成）
- POST /api/ai/suggest_tags（タグ提案）
- POST /api/ai/generate_slug（スラッグ生成）
- POST /api/ai/generate_seo_meta（SEOメタ生成）

---

### 3. E2Eテスト導入（15件）

#### 3.1 Capybara + Selenium設定

**設定内容**:
- Capybara gemのインストール
- Selenium WebDriverの設定
- ヘッドレスChrome設定
- スクリーンショット機能
- 待機時間設定（5秒）

**ドライバー設定**:
```ruby
Capybara.register_driver :selenium_headless do |app|
  options = Selenium::WebDriver::Chrome::Options.new
  options.add_argument('--headless')
  options.add_argument('--no-sandbox')
  options.add_argument('--disable-dev-shm-usage')
  options.add_argument('--window-size=1920,1080')

  Capybara::Selenium::Driver.new(app, browser: :chrome, options: options)
end
```

#### 3.2 記事作成フローE2Eテスト（10件）

**記事作成（5件）**:
1. ログイン → 記事作成ページ
2. タイトル提案 → 適用
3. 要約生成 → 適用
4. タグ提案 → 適用
5. 保存 → 確認

**AI機能使用（5件）**:
1. タイトル提案ボタンクリック
2. ローディング表示確認
3. 提案結果表示確認
4. 提案適用確認
5. エラーハンドリング確認

#### 3.3 画像・メディアフローE2Eテスト（5件）

**画像アップロード（3件）**:
1. 画像選択 → アップロード
2. トリミング → 保存
3. 記事に挿入 → 確認

**メディアライブラリ（2件）**:
1. 画像一覧表示 → 編集
2. 画像削除 → 確認

---

## 🧪 テスト戦略

### テストピラミッド

```
        /\
       /E2E\          10% - 15テスト
      /------\
     /統合テスト\      30% - 60テスト
    /----------\
   /ユニットテスト\    60% - 75テスト
  /--------------\
```

### カバレッジ目標

| カテゴリ | 目標 | テスト数 | 実行時間 |
|---------|------|---------|---------|
| ユニットテスト | 90%以上 | 75件 | 5分以内 |
| 統合テスト | 80%以上 | 60件 | 10分以内 |
| E2Eテスト | 主要フロー | 15件 | 15分以内 |
| **合計** | **85%以上** | **150件** | **30分以内** |

### SimpleCov設定

```ruby
SimpleCov.start 'rails' do
  add_filter '/spec/'
  add_filter '/config/'
  add_filter '/vendor/'

  add_group 'Models', 'app/models'
  add_group 'Controllers', 'app/controllers'
  add_group 'Services', 'app/services'
  add_group 'Helpers', 'app/helpers'
  add_group 'Jobs', 'app/jobs'

  minimum_coverage 85
  minimum_coverage_by_file 70
end
```

---

## 🔧 CI/CD統合

### GitHub Actions設定

```yaml
name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16-alpine
      redis:
        image: redis:7.4.1-alpine
    
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.7
          bundler-cache: true
      
      - name: Setup Database
        run: |
          bundle exec rails db:create
          bundle exec rails db:schema:load
      
      - name: Run Tests
        run: bundle exec rspec
      
      - name: Upload Coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/coverage.json
```

### テスト実行コマンド

```bash
# 全テスト
bundle exec rspec

# カバレッジ測定
COVERAGE=true bundle exec rspec

# ユニットテストのみ
bundle exec rspec spec/services spec/models

# 統合テストのみ
bundle exec rspec spec/requests

# E2Eテストのみ
bundle exec rspec spec/system
```

---

## 📦 必要なGem

```ruby
# Gemfile
group :test do
  gem 'rspec-rails', '~> 6.1'
  gem 'factory_bot_rails', '~> 6.4'
  gem 'faker', '~> 3.2'
  gem 'capybara', '~> 3.39'
  gem 'selenium-webdriver', '~> 4.15'
  gem 'webmock', '~> 3.19'
  gem 'simplecov', '~> 0.22', require: false
end
```

---

## 🔗 関連ドキュメント

### 仕様書
- [要件定義](.kiro/specs/phase-5.7-test-coverage/requirements.md)
- [設計書](.kiro/specs/phase-5.7-test-coverage/design.md)
- [タスクリスト](.kiro/specs/phase-5.7-test-coverage/tasks.md)
- [テスト仕様書](.kiro/specs/phase-5.7-test-coverage/test_spec.md)

### 関連Phase
- Phase 5.2: AI機能実装
- Phase 5.1: メディアライブラリ
- Phase 5.6: パフォーマンス最適化

---

## 📈 成功指標

### 定量指標
- [ ] テストカバレッジ: 85%以上
- [ ] テスト総数: 150件以上
- [ ] テスト実行時間: 30分以内
- [ ] CI/CD統合完了

### 定性指標
- [ ] テストの保守性向上
- [ ] バグ検出率向上
- [ ] リファクタリングの安全性向上
- [ ] 開発速度の向上

---

## 🚧 制約事項

### 技術的制約
- E2Eテストの実行時間
- Selenium WebDriverの安定性
- CI/CD環境でのブラウザ実行
- テストデータの管理

### ビジネス的制約
- 実装期間: 6日間
- Phase 6（セキュリティ強化）との調整
- 既存機能への影響最小化

---

**作成日**: 2026-01-18  
**最終更新**: 2026-01-18  
**作成者**: Kiro  
**ステータス**: レビュー待ち
