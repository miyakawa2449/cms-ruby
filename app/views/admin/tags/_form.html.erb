<%= form_with(model: [:admin, tag]) do |form| %>
  <% if tag.errors.any? %>
    <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded mb-6">
      <h3 class="font-bold">エラーが発生しました:</h3>
      <ul class="mt-2 list-disc list-inside text-sm">
        <% tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="bg-white shadow rounded-lg p-6">
    <div class="space-y-6">
      <!-- 基本情報 -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <%= form.label :name, "タグ名", class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :name, 
              class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm",
              placeholder: "例: Ruby" %>
        </div>

        <div>
          <%= form.label :slug, "URL スラッグ", class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :slug, 
              class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm",
              placeholder: "例: ruby" %>
          <p class="mt-1 text-sm text-gray-500">URLで使用される部分です。英数字とハイフンのみ使用可能。</p>
        </div>
      </div>

    </div>

    <div class="mt-6 flex justify-end space-x-3">
      <%= link_to "キャンセル", admin_tags_path, 
                  class: "bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      <%= form.submit tag.persisted? ? "更新する" : "作成する", 
                      class: "bg-blue-600 py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
    </div>
  </div>
<% end %>