管理画面URL変更のお知らせ
========================

管理画面のURLが変更されました。

変更者: <%= @admin_user.email %>
変更種別: <%= @change_type.humanize %>
旧URL: /<%= @old_path %>
新URL: /<%= @new_path %>
変更日時: <%= @changed_at.strftime('%Y-%m-%d %H:%M:%S %Z') %>

重要: ブックマークの更新をお願いします。
変更に心当たりがない場合は、直ちに管理者へご連絡ください。

---
このメールはポートフォリオサイトからの自動送信です。
