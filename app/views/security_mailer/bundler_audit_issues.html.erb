<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #f44336; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; background: #f9f9f9; }
    .info-box { background: white; padding: 15px; margin: 10px 0; border-left: 4px solid #f44336; }
    .label { font-weight: bold; color: #666; }
    .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>セキュリティアラート: bundler-audit</h1>
    </div>
    <div class="content">
      <p>bundler-audit により <%= @issues_count %> 件の脆弱性が検出されました。</p>

      <div class="info-box">
        <p><span class="label">監査種別:</span> bundler-audit</p>
        <p><span class="label">ステータス:</span> <%= @audit_log.status.humanize %></p>
        <p><span class="label">検出数:</span> <%= @issues_count %></p>
        <p><span class="label">実行日時:</span> <%= @audit_log.created_at.strftime('%Y-%m-%d %H:%M:%S %Z') %></p>
      </div>

      <% if @issues.present? %>
      <h3>脆弱性一覧（先頭 <%= @issues.size %> 件）</h3>
      <% @issues.each do |issue| %>
        <div class="info-box">
          <p><span class="label">Gem:</span> <%= issue['gem'] || issue['name'] || '不明' %></p>
          <p><span class="label">CVE:</span> <%= issue['cve'] || issue['advisory'] || 'N/A' %></p>
          <% if issue['url'] %>
          <p><span class="label">URL:</span> <%= issue['url'] %></p>
          <% end %>
        </div>
      <% end %>
      <% end %>

      <p><strong>対応要:</strong> 速やかに該当Gemの更新をご検討ください。</p>
    </div>
    <div class="footer">
      <p>このメールはポートフォリオサイトからの自動送信です。</p>
    </div>
  </div>
</body>
</html>
